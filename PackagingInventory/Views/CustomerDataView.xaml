<UserControl x:Class="PackagingInventory.Views.CustomerDataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="0,0,0,15">
            <TextBlock Text="Select Customer: " VerticalAlignment="Center" FontSize="16" Margin="0,0,10,0"/>
            <ComboBox x:Name="CustomerDropdown" Width="250" SelectionChanged="CustomerDropdown_SelectionChanged"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="📦 Boxes Sold" FontSize="18" FontWeight="Bold" Margin="0,0,0,5"/>
                <DataGrid x:Name="SalesGrid" AutoGenerateColumns="False" Height="250" Margin="0,0,0,15">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="120"/>
                        <DataGridTextColumn Header="Box Types" Binding="{Binding BoxTypes}" Width="*"/>
                        <DataGridTextColumn Header="Amount" Binding="{Binding Amount}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="20,0,0,0">
                <TextBlock Text="💰 Payment History" FontSize="18" FontWeight="Bold" Margin="0,0,0,5"/>
                <DataGrid x:Name="PaymentGrid" AutoGenerateColumns="False" Height="250">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="120"/>
                        <DataGridTextColumn Header="Amount" Binding="{Binding Amount}" Width="100"/>
                        <DataGridTextColumn Header="Mode" Binding="{Binding Mode}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <StackPanel Margin="0,0,40,0">
                <TextBlock Text="Total Sale Value" FontSize="16" FontWeight="Bold"/>
                <TextBlock x:Name="TotalSaleText" FontSize="20"/>
            </StackPanel>
            <StackPanel Margin="0,0,40,0">
                <TextBlock Text="Payment Received" FontSize="16" FontWeight="Bold"/>
                <TextBlock x:Name="PaymentReceivedText" FontSize="20"/>
            </StackPanel>
            <StackPanel>
                <TextBlock Text="Outstanding" FontSize="16" FontWeight="Bold"/>
                <TextBlock x:Name="OutstandingText" FontSize="20" Foreground="Red"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
