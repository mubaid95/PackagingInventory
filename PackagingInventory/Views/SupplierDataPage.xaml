<UserControl x:Class="PackagingInventory.Views.SupplierDataPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">

    <Grid Background="#F8F8F8" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Supplier selection -->
        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="0,0,0,20">
            <TextBlock Text="Select Supplier:" FontWeight="Bold" FontSize="16" VerticalAlignment="Center"/>
            <ComboBox x:Name="SupplierComboBox" Width="200" Margin="10,0,0,0"
                      SelectionChanged="SupplierComboBox_SelectionChanged"/>
        </StackPanel>

        <!-- Summary Section -->
        <Border Grid.Row="1" Background="#FFFFFF" CornerRadius="10" Padding="15" Margin="0,10,0,0" BorderBrush="#DDD" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Supplier Summary" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Total Purchase Value:" FontSize="16" Width="200"/>
                    <TextBlock x:Name="TotalPurchaseText" FontSize="16" FontWeight="Bold"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Total Payment Made:" FontSize="16" Width="200"/>
                    <TextBlock x:Name="TotalPaymentText" FontSize="16" FontWeight="Bold"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Outstanding Amount:" FontSize="16" Width="200"/>
                    <TextBlock x:Name="OutstandingText" FontSize="16" FontWeight="Bold" Foreground="Red"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Payment History -->
        <GroupBox Header="Payment History" Grid.Row="2" FontWeight="Bold" Margin="0,10,0,10">
            <DataGrid x:Name="PaymentHistoryGrid" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="120"/>
                    <DataGridTextColumn Header="Mode" Binding="{Binding Mode}" Width="100"/>
                    <DataGridTextColumn Header="Amount" Binding="{Binding Amount}" Width="150"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <!-- Purchase History -->
        <GroupBox Header="Purchase History" Grid.Row="3" FontWeight="Bold" Margin="0,10,0,10">
            <DataGrid x:Name="PurchaseHistoryGrid" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="120"/>
                    <DataGridTextColumn Header="Box Details" Binding="{Binding BoxTypeDisplay}" Width="*"/>
                    <DataGridTextColumn Header="Amount" Binding="{Binding Amount}" Width="150"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </Grid>
</UserControl>
